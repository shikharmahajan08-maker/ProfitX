import { Plus, Search, Star, Trash2 } from "lucide-react";
import { Link } from "wouter";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { getStock, MARKET_STOCKS } from "@shared/marketData";
import { useTradingStore } from "@/store/tradingStore";
import { formatINR } from "@/utils/format";

export default function Watchlist() { const { watchlist, addToWatchlist, removeFromWatchlist } = useTradingStore(); const [query, setQuery] = useState(""); const matches = MARKET_STOCKS.filter((stock) => !watchlist.includes(stock.symbol) && `${stock.symbol} ${stock.companyName}`.toLowerCase().includes(query.toLowerCase())).slice(0, 5); return <div className="space-y-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-[#6f7cff]">Keep an eye on it</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-[#202743]">Watchlist</h1><p className="mt-1 text-sm text-slate-500">Your saved instruments, ready for the next research session.</p></div><div className="relative w-full sm:w-[260px]"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Add a stock..." className="h-10 rounded-xl border-slate-200 bg-white pl-10 text-xs" />{query && matches.length > 0 && <div className="absolute top-12 z-10 w-full overflow-hidden rounded-xl border border-slate-100 bg-white p-1 shadow-xl">{matches.map((stock) => <button key={stock.symbol} onClick={() => { addToWatchlist(stock.symbol); setQuery(""); }} className="flex w-full items-center gap-2 rounded-lg p-2 text-left hover:bg-slate-50"><div className="flex h-7 w-7 items-center justify-center rounded-lg text-[9px] font-bold" style={{ background: `${stock.accent}1a`, color: stock.accent }}>{stock.symbol.slice(0, 2)}</div><span className="text-xs font-semibold text-slate-700">{stock.symbol}</span><Plus className="ml-auto h-3.5 w-3.5 text-[#6f7cff]" /></button>)}</div>}</div></div><Card className="border-0 shadow-[0_12px_40px_rgba(25,31,54,0.05)]"><CardHeader className="flex flex-row items-center justify-between"><div className="flex items-center gap-2"><Star className="h-5 w-5 fill-[#e6a64a] text-[#e6a64a]" /><CardTitle className="text-lg">Default watchlist</CardTitle></div><span className="text-xs text-slate-400">{watchlist.length} instruments</span></CardHeader><CardContent><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{watchlist.map((symbol) => { const stock = getStock(symbol)!; const change = stock.price - stock.previousClose; return <div key={symbol} className="flex items-center justify-between rounded-2xl border border-slate-100 p-4"><Link href={`/stocks/${symbol}`} className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl text-xs font-bold" style={{ background: `${stock.accent}1a`, color: stock.accent }}>{symbol.slice(0, 2)}</div><div><p className="text-sm font-bold text-slate-800">{symbol}</p><p className="max-w-[125px] truncate text-[10px] text-slate-400">{stock.companyName}</p><p className={`mt-1 text-[10px] font-semibold ${change >= 0 ? "text-emerald-600" : "text-rose-500"}`}>{change >= 0 ? "+" : ""}{((change / stock.previousClose) * 100).toFixed(2)}%</p></div></Link><div className="text-right"><p className="text-sm font-semibold text-slate-800">{formatINR(stock.price)}</p><p className="mt-2 text-[10px] text-slate-400">{stock.sector}</p><button onClick={() => removeFromWatchlist(symbol)} aria-label={`Remove ${symbol}`} className="mt-3 text-slate-300 hover:text-rose-500"><Trash2 className="ml-auto h-3.5 w-3.5" /></button></div></div> })}</div>{watchlist.length === 0 && <div className="py-16 text-center"><Star className="mx-auto h-8 w-8 text-slate-300" /><p className="mt-3 text-sm font-semibold text-slate-700">Your watchlist is empty</p><p className="mt-1 text-xs text-slate-400">Search above or explore markets to add an instrument.</p><Link href="/markets"><Button className="mt-4 rounded-xl bg-[#6f7cff] text-xs">Explore markets</Button></Link></div>}</CardContent></Card></div>; }
