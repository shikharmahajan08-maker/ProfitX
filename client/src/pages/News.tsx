import { ExternalLink, Newspaper, Tag } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { DEV_NEWS } from "@shared/marketData";

export default function News() { const [sector, setSector] = useState("All"); const sectors = ["All", "IT", "Energy", "Banking", "Healthcare"]; const news = DEV_NEWS.filter((item) => sector === "All" || item.sector === sector); return <div className="space-y-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-[#6f7cff]">Stay curious</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-[#202743]">Market news</h1><p className="mt-1 text-sm text-slate-500">A lightweight development feed, ready for a real provider later.</p></div><div className="flex items-center gap-2 rounded-xl bg-[#fff4df] px-3 py-2 text-[10px] font-semibold text-[#a16e1d]"><Newspaper className="h-3.5 w-3.5" />Mock development provider</div></div><div className="flex flex-wrap gap-2">{sectors.map((item) => <button key={item} onClick={() => setSector(item)} className={`rounded-lg px-3 py-2 text-xs font-semibold ${sector === item ? "bg-[#202743] text-white" : "bg-white text-slate-500 shadow-sm"}`}>{item}</button>)}</div><div className="grid gap-4 lg:grid-cols-2">{news.map((item, index) => <Card key={item.id} className="border-0 shadow-[0_12px_40px_rgba(25,31,54,0.05)]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><Badge className="border-0 bg-[#f1f3ff] text-[10px] text-[#6472dc]"><Tag className="mr-1 h-3 w-3" />{item.sector}</Badge><span className="text-[10px] text-slate-400">{item.publishedAt}</span></div><h2 className="mt-5 text-lg font-semibold leading-6 tracking-tight text-[#202743]">{item.title}</h2><p className="mt-3 text-sm leading-6 text-slate-500">{item.description}</p><div className="mt-6 flex items-center justify-between border-t border-slate-100 pt-4"><div><p className="text-xs font-semibold text-slate-700">{item.source}</p>{item.symbol && <Link href={`/stocks/${item.symbol}`} className="text-[10px] text-[#6f7cff]">Related: {item.symbol}</Link>}</div><button className="flex items-center gap-1 text-[10px] font-semibold text-slate-400 hover:text-[#6f7cff]"><ExternalLink className="h-3.5 w-3.5" />Source link</button></div></CardContent></Card>)}<div className="rounded-2xl border border-dashed border-slate-200 bg-white p-10 text-center lg:col-span-2"><p className="text-sm font-semibold text-slate-700">No sentiment analysis in V1</p><p className="mx-auto mt-1 max-w-lg text-xs leading-5 text-slate-400">This feed intentionally contains no AI sentiment, recommendations, or generated claims. Connect a verified backend news provider before production.</p></div></div></div>; }
