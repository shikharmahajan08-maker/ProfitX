import { Search, SlidersHorizontal, Star, StarOff, TrendingUp } from "lucide-react";
import { Link } from "wouter";
import { useMemo, useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { SectionHeading } from "@/components/trading/TradingWidgets";
import { MARKET_STOCKS } from "@shared/marketData";
import { useTradingStore } from "@/store/tradingStore";
import { formatINR, formatNumber } from "@/utils/format";

export default function Markets() {
  const [query, setQuery] = useState(""); const [sector, setSector] = useState("All sectors");
  const { watchlist, addToWatchlist, removeFromWatchlist } = useTradingStore();
  const sectors = ["All sectors", ...Array.from(new Set(MARKET_STOCKS.map((stock) => stock.sector)))];
  const filtered = useMemo(() => MARKET_STOCKS.filter((stock) => { const matchesQuery = !query || `${stock.symbol} ${stock.companyName}`.toLowerCase().includes(query.toLowerCase()); return matchesQuery && (sector === "All sectors" || stock.sector === sector); }), [query, sector]);
  return <div className="space-y-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-[#6f7cff]">Discover</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-[#202743]">Markets</h1><p className="mt-1 text-sm text-slate-500">Explore simulated quotes from the development provider.</p></div><div className="flex items-center gap-2 rounded-xl bg-[#fff4df] px-3 py-2 text-[10px] font-semibold text-[#a16e1d]"><TrendingUp className="h-3.5 w-3.5" />Prices are simulated</div></div><Card className="border-0 shadow-[0_12px_40px_rgba(25,31,54,0.05)]"><CardContent className="flex flex-col gap-3 p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search symbol or company name" className="h-10 border-slate-200 bg-slate-50 pl-10 text-sm" /></div><div className="flex gap-2 overflow-x-auto">{sectors.map((item) => <button key={item} onClick={() => setSector(item)} className={`whitespace-nowrap rounded-lg px-3 py-2 text-xs font-semibold transition-colors ${sector === item ? "bg-[#202743] text-white" : "bg-slate-50 text-slate-500 hover:bg-slate-100"}`}>{item}</button>)}</div><Button variant="outline" className="hidden h-10 border-slate-200 sm:flex"><SlidersHorizontal className="mr-2 h-3.5 w-3.5" />Filters</Button></CardContent></Card><div><SectionHeading eyebrow={`${filtered.length} instruments`} title="India large-cap watch" action={<span className="text-xs text-slate-400">NSE • INR</span>} /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{filtered.map((stock) => { const change = stock.price - stock.previousClose; const isWatching = watchlist.includes(stock.symbol); return <Card key={stock.symbol} className="group border-0 shadow-[0_12px_40px_rgba(25,31,54,0.05)] transition-transform hover:-translate-y-0.5"><CardContent className="p-5"><div className="flex items-start justify-between"><Link href={`/stocks/${stock.symbol}`}><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl text-xs font-bold" style={{ background: `${stock.accent}1a`, color: stock.accent }}>{stock.symbol.slice(0, 2)}</div><div><p className="text-sm font-bold text-slate-800">{stock.symbol}</p><p className="max-w-[140px] truncate text-[10px] text-slate-400">{stock.companyName}</p></div></div></Link><button onClick={() => isWatching ? removeFromWatchlist(stock.symbol) : addToWatchlist(stock.symbol)} aria-label={isWatching ? `Remove ${stock.symbol} from watchlist` : `Add ${stock.symbol} to watchlist`} className="text-slate-300 transition-colors hover:text-[#e6a64a]">{isWatching ? <Star className="h-4 w-4 fill-[#e6a64a] text-[#e6a64a]" /> : <StarOff className="h-4 w-4" />}</button></div><div className="mt-6 flex items-end justify-between"><div><p className="text-xl font-semibold tracking-tight text-slate-900">{formatINR(stock.price)}</p><p className={`mt-1 text-[11px] font-semibold ${change >= 0 ? "text-emerald-600" : "text-rose-500"}`}>{change >= 0 ? "+" : ""}{change.toFixed(2)} ({((change / stock.previousClose) * 100).toFixed(2)}%)</p></div><span className="rounded-md bg-slate-50 px-2 py-1 text-[10px] font-medium text-slate-400">{stock.sector}</span></div><div className="mt-5 flex justify-between border-t border-slate-100 pt-3 text-[10px] text-slate-400"><span>Vol {formatNumber(stock.volume)}</span><span>Cap {stock.marketCap}</span></div></CardContent></Card> })}</div>{!filtered.length && <div className="rounded-2xl border border-dashed border-slate-200 bg-white p-14 text-center"><Search className="mx-auto h-7 w-7 text-slate-300" /><p className="mt-4 text-sm font-semibold text-slate-700">No instruments found</p><p className="mt-1 text-xs text-slate-400">Try a different symbol, company, or sector.</p></div>}</div></div>;
}
