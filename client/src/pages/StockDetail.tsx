import { ArrowLeft, BellPlus, ChartNoAxesCombined, ChevronDown, Star, StarOff, TrendingDown, TrendingUp } from "lucide-react";
import { Link, useRoute } from "wouter";
import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { PriceChart } from "@/components/trading/TradingWidgets";
import { generateHistory, getStock } from "@shared/marketData";
import { trpc } from "@/lib/trpc";
import { formatINR, formatNumber } from "@/utils/format";

export default function StockDetail() {
  const [, params] = useRoute("/stocks/:symbol");  const symbol = params?.symbol?.toUpperCase() ?? "TCS";
  const stock = getStock(symbol) ?? getStock("TCS")!;
  const [range, setRange] = useState("1M");
  
  const utils = trpc.useUtils();
  const { data: watchlist = [] } = trpc.account.watchlist.useQuery();
  const addToWatchlist = trpc.account.addToWatchlist.useMutation({ onSuccess: () => utils.account.watchlist.invalidate() });
  const removeFromWatchlist = trpc.account.removeFromWatchlist.useMutation({ onSuccess: () => utils.account.watchlist.invalidate() });
  
  const isWatching = watchlist.includes(stock.symbol); const change = stock.price - stock.previousClose; const history = useMemo(() => generateHistory(stock, { "1D": 24, "1W": 14, "1M": 30, "6M": 42, "1Y": 52, "5Y": 60 }[range] ?? 30), [stock, range]);
  return <div className="space-y-7"><Link href="/markets" className="inline-flex items-center gap-2 text-xs font-semibold text-slate-500 hover:text-slate-800"><ArrowLeft className="h-3.5 w-3.5" />Back to markets</Link><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div className="flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-2xl text-base font-bold" style={{ background: `${stock.accent}1b`, color: stock.accent }}>{stock.symbol.slice(0, 2)}</div><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-3xl font-semibold tracking-tight text-[#202743]">{stock.companyName}</h1><Badge className="border-0 bg-slate-100 text-[10px] text-slate-500">{stock.exchange}</Badge></div><p className="mt-1 text-sm text-slate-500">{stock.symbol} · {stock.sector} · {stock.industry}</p></div></div><div className="flex gap-2"><button onClick={() => isWatching ? removeFromWatchlist.mutate({ symbol: stock.symbol }) : addToWatchlist.mutate({ symbol: stock.symbol })} className={`flex h-11 items-center justify-center gap-2 rounded-xl px-4 text-sm font-semibold transition-colors ${isWatching ? "bg-[#e6a64a]/10 text-[#e6a64a] hover:bg-[#e6a64a]/20" : "bg-slate-100 text-slate-600 hover:bg-slate-200"}`}><Star className={`h-4 w-4 ${isWatching ? "fill-current" : ""}`} />{isWatching ? "Watching" : "Watch"}</button><Link href={`/trade?symbol=${stock.symbol}`}><Button className="rounded-xl bg-[#6f7cff] text-xs shadow-lg shadow-[#6f7cff]/20 hover:bg-[#5968df]">Trade {stock.symbol}</Button></Link></div></div><div className="grid gap-4 xl:grid-cols-[1.55fr_0.8fr]"><Card className="border-0 shadow-[0_12px_40px_rgba(25,31,54,0.05)]"><CardHeader className="flex flex-row items-start justify-between pb-2"><div><div className="flex items-baseline gap-3"><CardTitle className="text-3xl">{formatINR(stock.price)}</CardTitle><span className={`flex items-center text-sm font-semibold ${change >= 0 ? "text-emerald-600" : "text-rose-500"}`}>{change >= 0 ? <TrendingUp className="mr-1 h-4 w-4" /> : <TrendingDown className="mr-1 h-4 w-4" />}{change >= 0 ? "+" : ""}{change.toFixed(2)} ({((change / stock.previousClose) * 100).toFixed(2)}%)</span></div><p className="mt-1 text-xs text-slate-400">As of 24 Jun 2026, 15:30 IST · Development quote</p></div><div className="flex items-center gap-1 rounded-lg bg-slate-50 p-1">{["1D", "1W", "1M", "6M", "1Y", "5Y"].map((item) => <button key={item} onClick={() => setRange(item)} className={`rounded-md px-2 py-1.5 text-[10px] font-semibold ${range === item ? "bg-white text-[#6f7cff] shadow-sm" : "text-slate-400"}`}>{item}</button>)}</div></CardHeader><CardContent className="pt-4"><PriceChart data={history} positive={change >= 0} height={320} /></CardContent></Card><div className="space-y-4"><Card className="border-0 shadow-[0_12px_40px_rgba(25,31,54,0.05)]"><CardHeader><CardTitle className="text-base">Market information</CardTitle></CardHeader><CardContent className="grid grid-cols-2 gap-y-5 text-xs"><Info label="Day high" value={formatINR(stock.dayHigh)} /><Info label="Day low" value={formatINR(stock.dayLow)} /><Info label="52-week high" value={formatINR(stock.week52High)} /><Info label="52-week low" value={formatINR(stock.week52Low)} /><Info label="Volume" value={formatNumber(stock.volume)} /><Info label="Market cap" value={stock.marketCap} /></CardContent></Card><Card className="border-0 bg-[#202743] text-white shadow-[0_12px_40px_rgba(25,31,54,0.12)]"><CardContent className="p-5"><div className="flex items-center gap-2 text-[#9ba6ff]"><ChartNoAxesCombined className="h-4 w-4" /><span className="text-[10px] font-bold uppercase tracking-[0.18em]">Paper signal</span></div><p className="mt-3 text-sm font-semibold">This is a development quote, not investment advice.</p><p className="mt-2 text-[11px] leading-5 text-slate-400">Use it to learn the workflow: research → decide → paper trade → review.</p><Link href={`/trade?symbol=${stock.symbol}`}><Button className="mt-4 h-9 w-full bg-white/10 text-xs text-white hover:bg-white/20">Open order ticket <ChevronDown className="ml-2 h-3.5 w-3.5 -rotate-90" /></Button></Link></CardContent></Card></div></div><Card className="border-0 bg-[#fff4df] shadow-none"><CardContent className="flex flex-col justify-between gap-3 p-4 sm:flex-row sm:items-center"><div className="flex items-center gap-3"><BellPlus className="h-5 w-5 text-[#b17a21]" /><div><p className="text-xs font-semibold text-[#7e5c20]">Want to keep an eye on {stock.symbol}?</p><p className="text-[11px] text-[#a58245]">Create a basic price alert from the Alerts page.</p></div></div><Link href="/alerts"><Button variant="outline" className="border-[#e9c987] bg-transparent text-xs text-[#8a6728]">Set alert</Button></Link></CardContent></Card></div>;
}
function Info({ label, value }: { label: string; value: string }) { return <div><p className="text-[10px] text-slate-400">{label}</p><p className="mt-1 font-semibold text-slate-700">{value}</p></div>; }
